# The book is written on Windows and built on Ubuntu: storing LF in the
# repository and letting everyone see what they please keeps a whole chapter
# from showing up as modified over a line ending.
* text=auto eol=lf

# The PDFs under books/ are in .gitignore, but should one get in with
# `git add -f`, let it get in as a binary.
*.pdf binary
